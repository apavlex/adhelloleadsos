<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/head') %>
</head>
<body class="bg-brand-bg min-h-screen text-brand-dark">
  <%- include('partials/navbar') %>
  <main class="max-w-3xl mx-auto px-6 pt-12 pb-24">
    <h1 class="font-display font-extrabold text-4xl text-brand-dark dark:text-white mb-2">7-day activation</h1>
    <p class="text-brand-muted mb-8">Complete one habit per day. Progress is saved for your account and auto-checks when you use the product.</p>
    <%- include('partials/activation-7day', { activation: typeof activation !== 'undefined' ? activation : null, fullPage: true }) %>
  </main>
  <%- include('partials/footer') %>
</body>
</html>
